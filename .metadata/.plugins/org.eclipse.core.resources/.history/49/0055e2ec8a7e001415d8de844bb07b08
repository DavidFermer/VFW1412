//create main window
var mainWindow = Ti.UI.createWindow({
	title: "My Main Window",
	backgroundColor: "#000"
});
//Main Page Buttons
var mainHeader = Ti.UI.createView({
	top: 15,
	height: 100,
	borderRadius: 10,
	title: "Main Header"
	
});

var listButton = Ti.UI.createButton({
	backgroundColor: "fff",
	borderWidth: 5,
	borderColor: "F28705",
	top: 100,
	height: 50,
	width: 150,
	borderRadius: 10,
	title: "List View",
	font: {fontSize: 18, fontWeight: "bold", fontFamily: "Helvitica", fontColor: "Red"}
});

var tableButton = Ti.UI.createButton({
	backgroundColor: listButton.backgroundColor,
	borderWidth: listButton.borderWidth,
	borderColor: listButton.borderColor,
	top: listButton.top + listButton.height + 100,
	width: listButton.width,
	height: listButton.height,
	borderRadius: listButton.borderRadius,
	title: "Table View",
	font: listButton.font
});

mainWindow.add(listButton, tableButton);
//open window
var views = require("table");

mainWindow.open();
