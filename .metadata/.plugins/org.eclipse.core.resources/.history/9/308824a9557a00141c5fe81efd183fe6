
//windows

var mainWindow = Ti.UI.createWindow({
	title: "My Main Window",
	backgroundColor: "orange"
});

//views
var headerView = Ti.UI.createView({
	backgroundColor: "#eec81b",
	borderRadius: 5,
	left: 5,
	right: 5,
	top: 25,
	height: 30
});

var nextView = Ti.UI.createView({
	backgroundColor: "#cd0000",
	borderRadius: headerView.borderRadius,
	height: 75,
	width: 75,
	bottom: 15,
	right: 15
});


var previousView = Ti.UI.createView({
	backgroundColor: "brown",
	borderRadius: headerView.borderRadius,
	height: nextView.height,
	width: nextView.width,
	bottom: nextView.bottom,
	left: nextView.right
});

// labels
var arrayLabel = 

//calls
mainWindow.add(headerView, nextView, previousView);
mainWindow.open();
